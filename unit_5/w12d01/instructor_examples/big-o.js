// const logFirstElem = (arrayParam) => {
//     console.log(arrayParam[10000000]);
// }
//
// logFirstElem([1,3,5,8,0]);

const logAllElems = (arrayParam) => {
    for(let i = 0; i < arrayParam.length; i++){
        console.log(arrayParam[i]);
        console.log(arrayParam[i]);
    }
}

// logAllElems(['foo'])
logAllElems([1,3,5,8,0])
