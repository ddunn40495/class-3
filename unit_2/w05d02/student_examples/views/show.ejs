<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Fruits show page</h1>
        The <%=allFruits.name; %> is  <%=allFruits.color; %>.
        <% if(allFruits.readyToEat === true){ %>
            It is ready to eat
        <% } else { %>
            It is not ready to eat
        <% } %>
    </body>
    <a href="/fruits">Back to Index</a>

</html>    